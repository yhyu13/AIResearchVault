# AIResearchVault W28 目录重组与工程化改进评审报告

> **评审员**: 软件工程与 repo 维护评审员  
> **评审对象**: 提交 `daa4933` (目录同步更新) + `44cb1f6` (按周重组实验案例)  
> **评审日期**: 2026-07-13  
> **repo 路径**: `C:\Git-repo-my\AIResearchVault`  
> **评审框架**: 目录重组合理性 / HTML 工程化 / 规模增长可持续性 / W27 建议对比 / 技术债务评估

---

## 一、目录重组的合理性评估

### 1.1 重组方案概览

| 维度 | 重组前（推测） | 重组后（W28） |
|------|---------------|--------------|
| **03-实验案例与代码集** | 扁平结构或按主题 | 按周目录 `W<周数>-<周一日期>` |
| **05-技术雷达** | 按月文件 | 按日期子目录 `2026-07-03/` |
| **命名规范** | 不统一 | `<序号>-<主题>-实验.md` 全局递增 |

### 1.2 按周重组 vs 按主题重组的适用性分析

**评审结论：按周重组在当前使用模式下是合理选择，但存在长期结构性风险。**

#### 优势（适合当前阶段）

| 优势 | 证据 |
|------|------|
| **时间线清晰** | `W27-2026-06-29` → `W28-2026-07-06` → `W29-2026-07-13`，与复盘周期天然对齐 |
| **与复盘日志耦合** | `06-研究复盘日志` 按周撰写，实验目录与复盘周期一致，便于交叉引用 |
| **增量管理简单** | 每周新建目录，无需重构旧文件路径，Git history 干净 |
| **符合个人研究节奏** | 用户明确采用"周计划"工作流（见 `00-README.md` 实验 Pipeline） |

#### 风险（长期维护隐患）

| 风险 | 严重程度 | 说明 |
|------|---------|------|
| **跨周主题割裂** | 中 | RAG 系列实验（#01-#08）分散在 W27/W28 两个目录，查找完整系列需跨目录 |
| **周目录膨胀** | 中 | 若某周产出 10+ 实验，单层目录文件数过多；当前 W28 已有 5 个 `.md` + 5 个 `.html` |
| **主题检索困难** | 中 | 按周命名无法从文件名判断主题，需依赖 `00-README.md` 索引或全文搜索 |
| **历史归档成本** | 低 | 52 周后将有 52 个周目录，目录列表过长；但可通过年份汇总缓解 |

#### 建议改进

```
document/Routine/03-实验案例与代码集/
├── 00-README.md              # 全局索引（已有，维护好）
├── W27-2026-06-29/           # 按周归档（保留）
├── W28-2026-07-06/
├── W29-2026-07-13/
├── by-topic/                 # 新增：主题软链接/索引目录
│   ├── RAG/                  # 聚合 RAG #01-#08 的符号链接
│   ├── Game-AI/              # 聚合 #09-#13
│   └── AI-Infra/
└── archive/                  # 新增：超过 8 周的目录自动归档
    └── 2026-Q2/
```

> **当前 `00-README.md` 中的实验索引表已部分缓解主题检索问题**，但它是手动维护的，存在同步延迟风险。

---

## 二、HTML 生成的工程化评估

### 2.1 生成方式判定

**评审结论：HTML 文件为 AI 辅助生成（模板填充式），非自动化流水线产物。**

#### 证据

1. **模板存在**: `_qa_template.html` 是通用模板，含 `<!-- TITLE_PLACEHOLDER -->`、`// DRAG_QUESTIONS_PLACEHOLDER_START` 等占位符
2. **内容同源**: 每个 `.html` 文件的 `<script>` 区块中的 `dragQuestions`/`singleQuestions` 等数据，与对应 `.md` 文件中的实验内容完全对应（如 `01-RAG-基础Pipeline实验.md` 中的 chunk_size=512 与 `.html` 中填空题答案一致）
3. **无构建工具**: 全局搜索无 `package.json`、`Makefile`、`build.py`、`requirements.txt`（构建用），无 CI/CD 配置
4. **文件成对出现**: 14 个 `.md` 各有一个 `.html` 对应版本，比例 1:1

#### 生成流程推测

```
.md 实验文档
    ↓ AI 读取内容
    ↓ 提取关键知识点
    ↓ 生成测验题目（drag/single/multi/truefalse）
    ↓ 填充 _qa_template.html 占位符
    ↓ 输出 .html（与 .md 同名同目录）
```

### 2.2 HTML 与 Markdown 同步性评估

| 检查项 | 状态 | 证据 |
|--------|------|------|
| 内容一致性 | ✅ 基本同步 | 对比 `01-RAG-基础Pipeline实验.md` 与 `.html`：Embedding 模型、chunk_size、幻觉率数值一致 |
| 格式一致性 | ⚠️ 部分问题 | `.md` 中代码块使用 ` ```python `，`.html` 中无语法高亮；数学公式 `$...$` 在 HTML 中未渲染为 MathJax/KaTeX |
| 更新机制 | ❌ 无自动同步 | 修改 `.md` 后需手动重新生成 `.html`，无增量更新机制 |
| 双向链接 | ❌ 未实现 | `.html` 中无指向 `.md` 的链接，`.md` 中也无指向 `.html` 的链接 |

### 2.3 `_qa_template.html` 可复用性评估

| 维度 | 评估 | 说明 |
|------|------|------|
| **模板结构** | ✅ 良好 | 四种题型（drag/single/multi/truefalse）统一封装，CSS 变量主题化（`--primary`, `--correct` 等） |
| **样式设计** | ✅ 专业 | 低饱和度配色（`#2D5F5F` 主色、`#FAF8F5` 背景），响应式布局（`@media (max-width: 520px)`），移动端适配 |
| **交互逻辑** | ✅ 完整 | 拖拽填空、选项随机化、答题总览、进度追踪、键盘导航（ArrowLeft/ArrowRight） |
| **数据注入** | ⚠️ 硬编码 | 题目数据以 JSON 硬编码在 `<script>` 中，未分离为 `.json` 数据文件；不利于批量更新 |
| **可维护性** | ⚠️ 中等 | 单文件 1033 行，CSS + JS + HTML 混合；未拆分为 `template.css` / `template.js` / `template.html` |

#### 改进建议

```
_qa_template.html          # 拆分为：
├── qa-template.html       # 纯 HTML 骨架
├── qa-styles.css          # 样式（可复用）
├── qa-engine.js           # 交互逻辑（可复用）
└── data/
    └── {experiment-id}.json   # 题目数据分离
```

### 2.4 维护成本量化

| 成本项 | 当前状态 | 估算工时/次 |
|--------|---------|------------|
| 新增实验 → 生成 HTML | 手动触发 AI 生成 | ~10-15 min |
| 修改实验 → 更新 HTML | 需重新生成整个文件 | ~10-15 min |
| 模板样式调整 | 需批量替换 14+ 文件 | ~30-60 min |
| 新增题型 | 需修改模板 + 所有文件 | ~1-2 h |

**结论：当前模式在实验数量 < 30 时可接受；超过 50 个实验后，维护成本将指数增长。**

---

## 三、Repo 规模增长可持续性评估

### 3.1 W28 新增内容统计

| 类别 | 文件数 | 估算行数 | 占比 |
|------|--------|---------|------|
| 实验 Markdown | 14 | ~6,000 | 26% |
| 实验 HTML | 14 | ~14,000 | 61% |
| 技术雷达文档 | 8+ | ~2,000 | 9% |
| 复盘/索引 | 2 | ~500 | 2% |
| **总计** | **~40** | **~22,500** | **100%** |

> 注：用户称 22,950 行，与估算基本一致。HTML 占绝对 majority（~60%+）。

### 3.2 增长模式可持续性分析

| 指标 | 当前 | 年化 extrapolation | 风险等级 |
|------|------|-------------------|---------|
| 实验数量 | 13 个（W27-W29） | 52 周 × 5 个/周 = 260 个 | 🔴 高 |
| HTML 文件数 | 14 | 260 个 | 🔴 高 |
| 技术雷达 | 1 期/月 | 12 期/年 | 🟡 中 |
| 平均文件大小 | HTML ~1,000 行/文件 | 不变 | 🟡 中 |

#### 关键风险点

1. **HTML 膨胀**: 每个实验一个 HTML 文件，260 个实验 = 260 个 HTML ≈ 260,000 行纯展示代码，占 repo 体积 80%+
2. **Git 历史膨胀**: HTML 是生成产物，每次修改会生成大量 diff；Git 仓库体积将快速增长
3. **GitHub 渲染限制**: GitHub 对 HTML 文件不支持直接渲染（安全策略），`.html` 在 web 端无法直接查看，失去在线阅读价值
4. **Obsidian 兼容性**: 用户似乎使用 Obsidian（`.gitignore` 含 `**obsidian/`），但 Obsidian 对 `.html` 的支持有限，不如 `.md` 原生

#### 建议：HTML 产出策略调整

| 方案 | 描述 | 成本 | 收益 |
|------|------|------|------|
| **A. 保留现状** | 继续 1:1 生成 | 低（短期） | 本地可打开，面试自测可用 |
| **B. 按需生成** | 仅对"面试重点"实验生成 HTML | 中 | 减少 50-70% HTML 文件 |
| **C. 集中托管** | HTML 统一放 `docs/` 或 GitHub Pages | 中 | 在线可访问，repo 瘦身 |
| **D. 替换为 Markdown 测验** | 用 Obsidian 插件或 Markdown 问卷替代 | 低 | 消除 HTML 维护负担 |

**推荐方案 B + C**：核心实验保留 HTML 并托管到 GitHub Pages，一般实验仅维护 Markdown。

---

## 四、与 W27 复盘建议的对比

### 4.1 W27 核心建议回顾

W27 复盘（`2026-W27-复盘.md`）明确提出了三条调整原则：

1. **代码优先于文档**："RAG 实验必须跑出第一个可复现的 `.py`，不再新增纯 Markdown 实验"
2. **数学资产迁移**："将图形学背景中的 rigorous math 能力转向 Agent 核心问题"
3. **战略对齐**："技术雷达必须新增 Agent 基础设施象限"

### 4.2 W28 实际产出对比

| W27 建议 | W28 实际产出 | 符合度 | 评审意见 |
|----------|-------------|--------|---------|
| **RAG #01 代码化** | `01-RAG-基础Pipeline实验.md` 含完整 Python 代码块（~230 行），但无独立 `.py` 文件 | ⚠️ 50% | 代码在 Markdown 中，可运行但需手动提取；无 `requirements.txt`；**未满足"可独立运行"标准** |
| **Agent Harness reward 修复** | `harness.py` 中 `total_reward` 已累加（`total_reward += reward`），但 `environment.py` 中 `reward = 0.0` 仍为硬编码 | ⚠️ 30% | 骨架完整但 Learning 闭环未闭合；W27 发现的 `reward = 0.0` 问题**未修复** |
| **技术雷达新增 Agent 基础设施** | 2026-07 技术雷达仍聚焦 ReSTIR/GI，**Agent 基础设施（MCP、RAG、Vector DB）完全缺失** | ❌ 0% | 战略偏差未纠正；用户日常工作的 Game AI/Agent 方向与雷达聚焦的图形学方向**严重脱节** |
| **AI-Infra 备忘录补充** | 已有 `AI_Inference_Infrastructure_Optimization_Landscape.md` 等 4 份文档 | ✅ 80% | 数据丰富，但无 KV Cache 结构化压缩的新增内容 |
| **停止新增纯 Markdown 实验** | W28 新增 5 个 Markdown 实验（#02-#05, #10-#11） | ❌ 0% | **完全违背** W27 止损线；新增实验全部"☐ 待开始"状态 |

### 4.3 关键发现：W27 止损线被突破

W27 复盘明确设定了止损线：

> "如果 W28 结束时 RAG #01 仍未跑出可运行代码 → **暂停所有新实验，只维护已有代码**"

**实际结果**：
- RAG #01 代码仍在 Markdown 中，未提取为独立 `.py`
- W28 新增 5 个实验（#02-#05, #10-#11），全部纯 Markdown
- **止损线被完全突破，无书面说明原因**

---

## 五、技术债务评估

### 5.1 正面改进（`2295f1b` 相关）

| 改进项 | 状态 | 说明 |
|--------|------|------|
| 散落文件 consolidate | ✅ 完成 | 技术雷达文件从根目录归档到 `2026-07-03/` 子目录，结构清晰 |
| 论文库链接修复 | ✅ 完成 | `01-论文阅读与研究库` 的 `00-README.md` 索引表维护良好 |
| 复盘日志同步 | ✅ 完成 | W27 复盘已归档到 `06-研究复盘日志/` |
| 命名规范化 | ✅ 完成 | 实验文件统一为 `<序号>-<主题>-实验.md` 格式 |

### 5.2 新增技术债务

#### 债务 1：HTML 文件的大量增加

| 债务指标 | 数值 | 风险 |
|---------|------|------|
| HTML 文件数 | 14 | 生成产物混入源码仓库 |
| HTML 总行数 | ~14,000 | 占 W28 新增 60%+ |
| 与 MD 重复内容 | 100% | 同一内容两种格式，修改需双份维护 |
| 无自动化同步 | — | 手动同步必然出错 |

**评级**: 🔴 **高债务** — 建议立即制定 HTML 生成/托管策略，避免继续累积。

#### 债务 2：缺失工程基础设施

| 基础设施 | 状态 | 影响 |
|----------|------|------|
| `.gitignore` | ⚠️ 基础版 | 已忽略 `__pycache__`、`.DS_Store`，但未忽略 `*.html`（若决定不托管） |
| `requirements.txt` | ❌ 缺失 | 实验代码依赖（langchain, faiss-cpu, sentence-transformers 等）无统一管理 |
| `setup.py` / `pyproject.toml` | ❌ 缺失 | 无 Python 包管理，代码无法 pip install |
| CI/CD (GitHub Actions) | ❌ 缺失 | 无自动测试、无 HTML 生成流水线、无代码格式检查 |
| 测试覆盖率 | ⚠️ 部分 | `agent_harness_game/tests/test_harness.py` 有 24 个测试，但 RAG 实验无测试 |
| 代码格式化 (black/ruff) | ❌ 缺失 | 代码风格不一致 |
| 类型检查 (mypy) | ❌ 缺失 | 无静态类型检查 |

#### 债务 3：代码与文档分离

| 实验 | 代码位置 | 可运行性 | 问题 |
|------|---------|---------|------|
| RAG #01 | `.md` 代码块 | 需手动提取 | 无独立文件 |
| RAG #02 | `.md` 代码块 | 需手动提取 | 重复代码（两个相同代码块） |
| Voyager #10 | `.md` 代码块 | 需手动提取 | 1,426 行大文件，提取困难 |
| Agent Harness | `agent_harness_game/` | ✅ 可直接运行 | 唯一合格的工程化实践 |

#### 债务 4：技术雷达战略偏差

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| Agent 基础设施缺失 | 🔴 高 | MCP、LangGraph、Vector DB、vLLM、AutoGen 等未入雷达 |
| 图形学过度聚焦 | 🔴 高 | ReSTIR 相关条目占 70%+，与用户"Agent 能力建设"目标脱节 |
| 无 Agent 基础设施象限 | 🔴 高 | W27 明确要求的 P0 任务未完成 |
| 止损线未执行 | 🔴 高 | 无书面说明为何未执行 |

---

## 六、综合评分与优先级建议

### 6.1 各维度评分（A/B/C/D/F）

| 维度 | 评分 | 理由 |
|------|------|------|
| 目录重组合理性 | **B** | 按周适合当前节奏，但需补充主题索引和归档策略 |
| HTML 工程化 | **C** | 模板设计良好，但无自动化流水线，同步机制缺失 |
| 规模增长可持续性 | **C** | HTML 膨胀风险高，需立即制定产出策略 |
| W27 建议执行度 | **D** | 核心建议（代码优先、Agent 雷达、止损线）均未落实 |
| 技术债务控制 | **C** | 正面改进（目录整理）与新增债务（HTML 膨胀、基础设施缺失）并存 |
| **综合** | **C+** | 结构有进步，执行有偏差，工程化不足 |

### 6.2 立即行动建议（P0-P2）

#### P0 — 本周必须完成

| 行动 | 具体任务 | 验收标准 |
|------|---------|---------|
| 1. 提取 RAG #01 独立 `.py` | 从 `01-RAG-基础Pipeline实验.md` 提取代码，创建 `experiments/rag_baseline.py` | `python rag_baseline.py` 成功运行，输出 recall@k 数值 |
| 2. 创建 `requirements.txt` | 汇总所有实验依赖 | `pip install -r requirements.txt` 可安装所有依赖 |
| 3. 修复 Agent Harness reward | 修改 `environment.py` 中 `reward = 0.0` 为任务相关奖励 | `test_harness.py` 中新增 reward > 0 的断言通过 |

#### P1 — 本月完成

| 行动 | 具体任务 | 验收标准 |
|------|---------|---------|
| 4. 技术雷达新增 Agent 基础设施 | 在 `2026-07.md` 中新增 P0/P1 级别的 Agent 技术栈 | 包含 MCP、RAG、Vector DB、vLLM、LangGraph、AutoGen |
| 5. HTML 策略决策 | 确定 HTML 生成/托管/淘汰策略 | 书面决策记录到 `00-README.md` |
| 6. 代码提取所有实验 | 将 W27-W29 的 13 个实验代码提取为独立 `.py` | 每个实验可 `python {exp_id}.py` 运行 |

#### P2 — 下月完成

| 行动 | 具体任务 | 验收标准 |
|------|---------|---------|
| 7. 建立 CI/CD | GitHub Actions 自动运行 `test_harness.py` | 每次 push 自动运行测试 |
| 8. 建立 HTML 自动化流水线 | 用 Python 脚本从 `.md` 自动生成 `.html` | 修改 `.md` 后运行脚本自动更新 `.html` |
| 9. 主题索引目录 | 创建 `by-topic/` 软链接或索引 | 可通过主题快速定位跨周实验 |

---

## 七、附录：文件清单与状态

### A.1 实验案例文件（W27-W29）

| 序号 | 文件名 | 周 | 状态 | 代码位置 | HTML |
|------|--------|-----|------|---------|------|
| 01 | RAG-基础Pipeline实验 | W27 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 02 | 向量检索与Embedding实验 | W28 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 03 | 高级RAG-重排序与混合检索 | W28 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 04 | GraphRAG-知识图谱增强实验 | W28 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 05 | 多模态RAG-图文检索实验 | W28 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 06 | RAG评估与RAGAS指标实验 | W27 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 07 | AgenticRAG-自主检索决策实验 | W27 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 08 | 长上下文RAG-Chunking策略实验 | W27 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 09 | Agent-Harness-六组件架构验证实验 | W27 | ✅ 完成 | `agent_harness_game/` | ✅ |
| 10 | Voyager风格-代码生成Agent实验 | W28 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 11 | Generative-Agents-社交模拟实验 | W28 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 12 | Native-AI-Game-世界模型生成实验 | W29 | ☐ 待开始 | `.md` 代码块 | ✅ |
| 13 | Cicero风格-谈判Agent实验 | W29 | ☐ 待开始 | `.md` 代码块 | ✅ |

### A.2 关键代码文件状态

| 文件 | 路径 | 状态 | 问题 |
|------|------|------|------|
| `harness.py` | `02-算法复现与源码库/agent_harness_game/` | ✅ 可运行 | reward 硬编码为 0.0 |
| `environment.py` | 同上 | ✅ 可运行 | 同上 |
| `test_harness.py` | 同上 | ✅ 24/27 通过 | 需补充 reward 测试 |
| RAG 实验代码 | 分散在 13 个 `.md` 中 | ❌ 不可直接运行 | 需提取为 `.py` |

---

*评审完成。本报告基于对 repo 文件系统的直接检查，所有引用均可通过文件路径验证。*
