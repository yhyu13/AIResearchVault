## Parameter-Efficient Fine-Tuning (PEFT) 可用于解决在下游任务中需要大规模语言模型进行微调的问题。

本文通过分类和对比分析PEFT的不同方法,说明了该方法的价值和效果。

### 创新点: 参数高效微调方法的提出。该方法通过只训练一小部分参数来解决在许多下游任务中需要大规模语言模型进行微调的不可行和不切实际的问题。

### 表现:

本文对多个下游任务进行了测试,并在除了两个任务(WiC和CoLA)外,取得了SOTA结果。PEFT方法在不增加太多参数的情况下提高了模型的效率和性能。

### 工作量:

文章提供了对近40篇使用不同PET方法的文章的概述和对比分析,为研究者提供了参考和指导。然而,由于本文没有提供具体算法的实现和代码,因此对于实际使用PET方法的读者而言,实现起来可能存在一定的难度。

## 高亮重点

### 本文提出的PEFT方法可用于解决在下游任务中需要大规模语言模型进行微调的问题。

该方法通过只训练一小部分参数来解决在许多下游任务中需要大规模语言模型进行微调的不可行和不切实际的问题。这些参数可以是现有模型参数的子集或一组新添加的参数。

PEFT方法有不同的参数效率、内存效率、训练速度、模型最终质量以及附加的推理成本。

本文对PEFT的分类进行了介绍,概述了近40篇使用不同PEFT的文章,并对一些主流PEFT方法进行了对比和分析。例如,文章对Residue Text Classification和GLUE任务中的一些具有代表性的模型进行了测试,在除了其中两个任务(WiC和CoLA)上均取得了SOTA结果。

PEFT方法在不增加太多参数的情况下可以提高模型效率和性能,因此可以在处理大规模任务时取得良好的效果。


---

1. Title: Scaling Down to Scale Up: A Guide to Parameter-Efﬁcient Fine-Tuning

2. Authors: Vladislav Lialin, Vijeta Deshpande, Anna Rumshisky

3. Affiliation: 贾富华大学(UMass Lowell)

4. Keywords: parameter-efficient fine-tuning, language models, efficiency, performance

5. Urls: Paper link: None, Github code link: None

6. Summary:
- (1):本文研究的背景是如何解决在很多下游任务上需要大规模语言模型进行微调的不可行和不切实际的问题。
- (2):过去的方法是fine-tuning，但是由于语言模型太大，只能微调一小部分参数，不适用于大规模任务。本文的方法是parameter-efficient fine-tuning(PEFT)。该方法旨在通过只训练一小组参数来解决此问题，这些参数可能是现有模型参数的子集或一组新添加的参数。方法有不同的参数效率、内存效率、训练速度、模型最终质量以及附加的推理成本。
- (3):本文提出了PEFT的分类，概述了近40篇使用不同PEFT的文章，并对一些主流PEFT方法进行了对比和分析。
- (4):文章对residue text classification和GLUE任务中的一些具有代表性的模型进行了测试，在除了其中两个任务（WiC和CoLA）上均取得了SOTA结果。PEFT方法在不增加太多参数的情况下可以提高模型效率和性能，因此可以在处理大规模任务时取得良好的效果。
7. Methods: 

- (1): 本文介绍了参数高效微调(Parameter-Efficient Fine-Tuning, PEFT)的方法，该方法通过只训练一小部分参数，旨在解决在很多下游任务中需要大规模语言模型进行微调的不可行和不切实际的问题。这些参数可以是现有模型参数的子集或一组新添加的参数。PEFT方法有不同的参数效率、内存效率、训练速度、模型最终质量以及附加的推理成本。

- (2): 本文对PEFT的分类进行了介绍，概述了近40篇使用不同PEFT的文章，并对一些主流PEFT方法进行了对比和分析。例如，文章对Residue Text Classification和GLUE任务中的一些具有代表性的模型进行了测试，在除了其中两个任务（WiC和CoLA）上均取得了SOTA结果。

- (3): 文章着重介绍了 PEFT 方法中基于适配器(Adapter)和基于软提示(Soft Prompt)等增量方法的思想及其实现。适配器是一种添加新参数的方法，它可以在模型的预训练层之间插入。软提示是一种将可训练张量附加到模型的输入以使其更容易适应新任务的方法，它通过优化张量参数以在新任务上表现出更好的性能。此外，文章还介绍了一些特殊的PEFT方法，如Intrinsic Prompt Tuning(IPT)、Ladder-Side Tuning等。其中，IPT使用自动编码器对软提示进行降维，以实现对未见任务参数的减少；Ladder-Side Tuning则是在预训练模型后端附加一个额外的小型transformer网络，以在不增加太多计算和内存负担的情况下进行微调。





8. Conclusion: 
- (1): 本文提出的参数高效微调方法(PET)可用于解决在下游任务中需要大规模语言模型进行微调的问题。文章通过分类和对比分析PEFT的不同方法，说明了该方法的价值和效果。

- (2): 创新点: 参数高效微调方法的提出。该方法通过只训练一小部分参数来解决在许多下游任务中需要大规模语言模型进行微调的不可行和不切实际的问题。

          表现: 本文对多个下游任务进行了测试，并在除了两个任务(WiC和CoLA)外，取得了SOTA结果。PEFT方法在不增加太多参数的情况下提高了模型的效率和性能。

          工作量: 文章提供了对近40篇使用不同PET方法的文章的概述和对比分析，为研究者提供了参考和指导。然而，由于本文没有提供具体算法的实现和代码，因此对于实际使用PET方法的读者而言，实现起来可能存在一定的难度。




